{"version":"0.2.0","body":"function main(workbook: ExcelScript.Workbook) {\n    // Specify the cell address and the prefix\n    const cellAddress = \"B9\"; // Adjust this to the cell you want to set\n    const prefix = \"PF-\"; // Adjust this to the desired prefix\n\n    // Get all the worksheets\n    const worksheets = workbook.getWorksheets();\n\n    // Iterate over the worksheets and set the cell value\n    worksheets.forEach((worksheet, index) => {\n        // Format the sequence number as two digits\n        const sequenceNumber = (index + 1).toString().padStart(2, '0');\n        // Set the cell value with the prefix and sequence number\n        worksheet.getRange(cellAddress).setValue(prefix + sequenceNumber);\n    });\n}","description":"","parameterInfo":"{\"originalParameterOrder\":[],\"parameterSchema\":{\"type\":\"object\",\"default\":{},\"x-ms-visibility\":\"internal\"},\"returnSchema\":{\"type\":\"object\",\"properties\":{}}}","apiInfo":"{\"variant\":\"synchronous\",\"variantVersion\":2}"}